{%- comment -%}
Icon Component
Usage: {% include "icon", name: "arrow", size: "24" %}

Parameters:
- name: アイコン名（必須）
- size: サイズ（デフォルト: 24）
- class: 追加のCSSクラス
- label: アクセシビリティ用のラベル
{%- endcomment -%}

{%- assign icon_size = size -%}
{%- unless icon_size -%}
{%- assign icon_size = "24" -%}
{%- endunless -%}
{%- assign icon_class = class -%}
{%- unless icon_class -%}
{%- assign icon_class = "" -%}
{%- endunless -%}

<svg class="icon -{{ name }} {{ icon_class }}" width="{{ icon_size }}" height="{{ icon_size }}" {%- if label %} aria-label="{{ label }}" {% else %} aria-hidden="true" {% endif -%}>
	<use href="/assets/icons/sprite.svg?v={{ timestamp }}#icon-{{ name }}"></use>
</svg>
